services:
  coros-music:
    build: .
    image: coros-music:local
    ports:
      - "8080:8080"
    environment:
      SPOTIFY_CLIENT_ID: ${SPOTIFY_CLIENT_ID}
      SPOTIFY_CLIENT_SECRET: ${SPOTIFY_CLIENT_SECRET}
    volumes:
      - ./coros-data:/root/.coros-music

volumes:
  coros-data:

